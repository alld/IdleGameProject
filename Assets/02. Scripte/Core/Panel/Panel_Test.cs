using IdleGame.Core.Popup;

public class Panel_Test : Base_AnimationPopup
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        _callback_OK();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
